NEXT_PUBLIC_CONVEX_URL=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
CLERK_JWT_ISSUER_DOMAIN=

# --- Convex environment (set via: npx convex env set KEY VALUE) ---
# GitHub PAT for repo fetching (5K req/hr)
# GITHUB_API_TOKEN=
# Voyage Code-2 embeddings (best for code retrieval)
# VOYAGE_AI_API_KEY=
# OpenAI API key (fallback embeddings + optional LLM)
# OPENAI_API_KEY=
# Anthropic API key (Claude for NL→BDD→TDD pipeline)
# ANTHROPIC_API_KEY=
# LLM provider: "anthropic" (default) or "openai"
# LLM_PROVIDER=anthropic
# LLM model: "claude-sonnet-4-5-20250929" (default)
# LLM_MODEL=claude-sonnet-4-5-20250929
# Qdrant vector store endpoint
# QDRANT_URL=
# Qdrant auth (if using Qdrant Cloud)
# QDRANT_API_KEY=
# Verification worker endpoint
# WORKER_API_URL=
# Shared secret for worker auth
# WORKER_API_SECRET=

# Shared secret for MCP server ↔ Convex auth (set in both Convex env and mcp-server/.env)
# MCP_SHARED_SECRET=

# --- Worker environment (worker/.env) ---
# CONVEX_URL=
# CONVEX_DEPLOY_KEY=
# WORKER_API_SECRET=
# REDIS_URL=redis://localhost:6379
# SONARQUBE_URL=http://localhost:9000
# SONARQUBE_TOKEN=
# GITHUB_API_TOKEN=

# --- MCP Server environment (mcp-server/.env) ---
# CONVEX_URL=                          # Convex deployment URL
# MCP_SHARED_SECRET=                   # Must match Convex env var above
# MCP_PORT=3002                        # HTTP transport port (default: 3002)
# MCP_TRANSPORT=stdio                  # "stdio" (local) or "http" (remote)
# GITHUB_BOT_TOKEN=                    # GitHub App/PAT with repo+org scope (for forks)
# GITHUB_MIRROR_ORG=                   # GitHub org for mirror forks (e.g. "arcagent-mirrors")
